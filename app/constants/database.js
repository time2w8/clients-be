const database = {
    HOST: 'localhost',
    USER: 'postgres', // Change credentials
    PASSWORD: 'root', // Change credentials
    DATABASE: "clients_db",
    TIMEOUT: 20000,
    PORT: 5432
}

module.exports = {
    database
}